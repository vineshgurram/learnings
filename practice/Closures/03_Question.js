// 3. Once Function (Medium)

// Write a function once(fn) that allows a function to be executed only once, even if called multiple times.

// function sayHello() { return "Hello!"; }
// const runOnce = once(sayHello);

// console.log(runOnce()); // "Hello!"
// console.log(runOnce()); // undefined
// console.log(runOnce()); // undefined
